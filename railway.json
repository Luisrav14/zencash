{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "node_modules/.bin/prisma migrate deploy && exec node_modules/.bin/next start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
